<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ユーザー管理 - 管理者画面</title>
    <link rel="stylesheet" href="/css/admin-style.css">
    <style>
        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0,0,0,0.4);
        }
        .modal-content {
            background-color: #fefefe;
            margin: 15% auto;
            padding: 20px;
            border: 1px solid #888;
            width: 80%;
        }
    </style>
</head>
<body>
    <header>
        <h1>ユーザー管理</h1>
        <a href="/admin">ダッシュボードに戻る</a>
    </header>

    <main>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>ユーザー名</th>
                    <th>パスワード</th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(user => { %>
                    <tr>
                        <td><%= user.id %></td>
                        <td><%= user.username %></td>
                        <td><%= user.password %></td>
                        <td>
                            <button class="edit-button" data-id="<%= user.id %>" data-username="<%= user.username %>">編集</button>
                            <button class="delete-button" data-id="<%= user.id %>">削除</button>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </main>

    <!-- 編集用モーダル -->
    <div id="editModal" class="modal">
        <div class="modal-content">
            <h2>ユーザー編集</h2>
            <form id="editForm">
                <input type="hidden" id="editUserId">
                <label for="editUsername">ユーザー名:</label>
                <input type="text" id="editUsername" required>
                <label for="editPassword">パスワード:</label>
                <input type="password" id="password" required>
                <button type="submit">更新</button>
                <button type="button" onclick="closeEditModal()">キャンセル</button>
            </form>
        </div>
    </div>

    <script src="/js/user.js"></script>
</body>
</html>