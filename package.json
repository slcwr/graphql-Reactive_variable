{
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@typescript-eslint/eslint-plugin": "^8.16.0",
    "@typescript-eslint/parser": "^8.16.0",
    "eslint": "^9.15.0",
    "wait-on": "^8.0.1"
  },
  "scripts": {
    "dev": "concurrently \"next dev\" \"tsx watch src/index.ts\"",
    "install-deps": "npm install --save-dev concurrently wait-on",
    "lint": "eslint ."
  }
}
